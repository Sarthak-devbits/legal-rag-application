[project]
name = "legal-rag-api"
version = "0.1.0"
description = "Legal Document Q&A — RAG system with FastAPI + Celery + pgvector"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.3",
    "pydantic-settings>=2.13.1",
    "uvicorn[standard]>=0.44.0",
]
